<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
     <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div  class="flex items-end w-full justify-end">
            <a class="bg-red-500 rounded-md px-3 py-3 text-sm inline-block mb-5" href="/logout">logout</a>
        </div>
        <h5 class="mb-5">edit the post</h5>
        <form action="/update/<%=post._id%>" method="post">
            <textarea placeholder="Whats on your mind" class="w-1/3 p-3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content"><%=post.content%></textarea>
            <input class="w-40 text-sm px-3 py-2 bg-yellow-500 block rounded-md mt-2" type="submit" value="Update Post">
        </form>

    </div>
</body>
</html>